const paddle1 = document.querySelector('.box1');
const paddle2 = document.querySelector('.box2');

const paddleMove = () => {
  document.addEventListener('keydown', (event) => {
    // Check which keys are pressed
    if (event.key === 'w') {
      // Move paddle1 up
      
    } else if (event.key === 's') {
      // Move paddle1 down
      
    } else if (event.key === 'ArrowUp') {
      // Move paddle2 up
      
    } else if (event.key === 'ArrowDown') {
      // Move paddle2 down
      
    }
  });
}
